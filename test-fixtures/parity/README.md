# Parity Fixtures

Canonical parity fixtures will live here once the Kotlin oracle capture flow is wired.

Each fixture should include:

- `id`
- `domain`
- `source_commit`
- `scenario`
- `setup`
- `request`
- `old_response`
- `expected_new_response`
- `must_match_fields`
- `intentional_differences`
- `waiver_reason`
- `owner`
